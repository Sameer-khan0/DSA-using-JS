// Array is collection of well defined values that has the same data type and store in liner format but in javasript we can store
// all the types of the data in array 

const arr = [312,321,434,42,432];
// arr.push(10);
// arr.pop();

console.log(arr);
